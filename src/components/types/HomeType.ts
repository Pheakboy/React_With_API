export interface HomeType {
    id: number;
    name: string;
    email: string;
    gender: string;
    status: string;
}

export interface HeaderType {
    contentType: string;
    Authorization: string;
}